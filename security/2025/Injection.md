# Injection
신뢰할 수 없는 사용자 입력이 인터프리터에 전달되어 해당 입력의 일부가 명령으로 실행되도록 하는 애플리케이션 결함입니다

### 취약한 경우

- 사용자 입력이 검증, 필터링, 무해화 되지 않는 경우
- 동적 쿼리 또는 매개변수화 되지 않은 호출이 인터프리터에 직접 사용되는 경우
- ORM 검색 파라미터에 정제되지 않은 데이터가 사용되는 경우
- 잠재적으로 위험한 데이터가 동적 쿼리나 명령에 직접 전달되는 경우

### 예방법

- 안전한 API 사용
- 서버 측 입력 유효성 검사
- 특수 문자 이스케이프