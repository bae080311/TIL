# Date()

날짜와 시간을 구할 수 있는 객체

```js
let now = new Date();
let nowYear = now.getFullYear(); // 연도
let nowMonth = now.getMonth() + 1; // 달
let nowDate = now.getDate(); // 일
let nowHours = now.getHours(); // 현재 시
let nowMins = now.getMinutes(); // 현재 분
let nowSec = now.getSeconds(); // 현재 초
```
